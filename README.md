# Tiny Kingdom Idle

Cozy incremental game (farming first; military & arcane later). Free, no ads — saves in your browser.

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

```bash
npm run build && npm start
```

for a production build.
